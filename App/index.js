import React from "react";
import { View, Text } from "react-native";

export default () => (
  <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
    <Text>View the finished branch for finished code.</Text>
  </View>
);
